name: CI Workflow


on:
  push:
    branches:
      - 'feature/**'
  pull_request:
    branches:
      - 'main'

jobs:
  ci-jobs:
    name: CI Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name:
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-pyspark.txt
      
      - name: Run pytest & generate HTML report
        run: |
          pytest \
            --disable-warnings \
            --cov=./src \
            --cov-report=html

      - name: Upload coverage HTML report
        uses: action/upload-artifact@v6
        with:
          name: coverage-html
          path: htmlcov