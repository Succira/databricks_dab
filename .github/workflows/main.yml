name: Demo Workflow

on:
  push:
    branches:
      - 'main'
      - 'feature/**'
jobs:
  demo-jobs:
    name: Demo Job
    runs-on: ubuntu-latest
    steps:
      - name: Message 1
        run: echo "This is message 1"
      - name: Mesage 2
        run: echo "Another message, 2"
